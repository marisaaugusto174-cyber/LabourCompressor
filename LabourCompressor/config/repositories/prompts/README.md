# Prompt Repository

Place prompt preset manifests in this directory as `*.json`.

Required fields:

- `id`
- `label`
- `filePath`

Prompt presets can also be referenced from taxonomy manifests through `promptLibraryPath`.
