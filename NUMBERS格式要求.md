# NUMBERS 格式要求

本文档定义当前项目在 `Apple Numbers` 场景下的正式输入、回填和归档联动规则。

目标是明确：

- `Numbers` 表格应该怎样准备
- 程序会读取哪些列
- 程序会把哪些内容写回哪些列
- 哪些信息属于多分支正式标签
- 哪些信息只允许由 `内容题材` 的唯一路径决定

---

## 1. 适用范围

本规范适用于以下流程：

- 导入 `.numbers` 表格
- 读取视频 URL 任务
- 下载视频
- 视频级打标
- 结构化标签回填
- 归档路径写回
- 按 `内容题材` 唯一路径归档

当前项目对 `.numbers` 的支持方式为：

- **读取**：程序直接读取 `.numbers` 文件结构
- **回填**：程序通过 `AppleScript` 写入 `Numbers` 可视单元格并保存

这意味着：

- 用户在 `Numbers` 前台能看到回填结果
- 回填结果不依赖“静默改底层文件后等待 Numbers 自己刷新”

---

## 2. 当前正式模板列结构

当前正式模板最少应包含以下列：

- `URL`
- `采集人`
- `归档状态`
- `一级标签`
- `二级标签`
- `三级标签`
- `四级标签`
- `归档路径`
- `归档文件名`

其中：

- `URL` 是任务输入列
- `采集人` 是业务记录列
- `归档状态 / 一级标签 / 二级标签 / 三级标签 / 四级标签 / 归档路径 / 归档文件名` 是程序正式回填列

---

## 3. 行结构要求

### 3.1 第 1 行

第 `1` 行必须是正式表头。

### 3.2 第 2 行

第 `2` 行允许作为模板说明行存在。

当前项目支持这种模式：

- 第 `2` 行写模板说明
- 第一列写类似 `请填写有效完整链接`
- 其他列写标签分类说明或人工提示

程序行为：

- 第 `2` 行不会被当作正式任务
- 模板提示行会自动跳过

### 3.3 第 3 行及以后

第 `3` 行开始才视为正式数据行。

每条有效任务至少需要：

- `URL`
- `采集人`

如果 `URL` 不是有效 `http/https` 链接，该行不会进入任务队列。

---

## 4. 标签体系与表格列的关系

### 4.1 0级标签

在业务语义上，模板中的这些大类属于 `0级标签`：

- `特殊标记`
- `情绪基调`
- `主体`
- `镜头语言分类`
- `环境和场景`
- `视觉风格`
- `内容题材`
- `时代与文化背景`
- `逻辑推理`

这些分支在识别层**不互斥**。

也就是说：

- 一条视频可以同时命中多个 `0级标签` 分支
- 每个分支下也可以命中多个正式标签

### 4.2 表格中的四级标签列

表格中的：

- `一级标签`
- `二级标签`
- `三级标签`
- `四级标签`

并不是只服务于某一个分支。  
它们保存的是**所有正式接受分支的结构化结果**。

当前回填规则：

- 同一级允许写多个值
- 多个值写在同一个单元格中
- 分隔符固定为：` | `
- 每个值都必须带 `0级标签` 前缀

示例：

```text
一级标签 = 内容题材: 广告营销 | 主体: 人物 | 情绪基调: 正向情绪
二级标签 = 内容题材: 产品广告 | 主体: 女性 | 环境和场景: 室内场景
三级标签 = 内容题材: 产品功能演示 | 主体: 单人
四级标签 = 主体: 日常动作
```

这样做的原因是：

- 目前程序不会自动给 `Numbers` 单元格着色
- 如果不写分支前缀，用户无法知道某个值属于哪条 `0级标签` 分支

---

## 5. 内容题材的特殊规则

`内容题材` 是当前正式归档主线。

这条分支和其他分支不同，必须遵守以下规则：

### 5.1 识别层

- 模型可以提出多个 `内容题材` 候选路径

### 5.2 正式记录层

- 系统会从正式接受的 `内容题材` 结果中收敛为**唯一一条最终路径**

### 5.3 回表层

- `内容题材` 仍然参与 `一级标签 / 二级标签 / 三级标签 / 四级标签` 的结构化回填
- 但在归档层，只允许保留这一条唯一正式路径

### 5.4 归档层

- 只有 `内容题材` 参与目录生成
- 其他 `0级标签` 不参与目录结构

---

## 6. 归档状态与归档路径规则

### 6.1 归档根目录

程序固定创建：

- `视频数据归档库`

其下固定只走：

- `内容题材`

完整归档根语义为：

- `视频数据归档库/内容题材`

### 6.2 归档路径来源

`归档路径` 只能来自 `内容题材` 的唯一正式路径。

示例：

如果唯一正式路径是：

```text
内容题材 > 广告营销 > 产品广告 > 产品功能演示
```

则 `归档路径` 写为：

```text
视频数据归档库/内容题材/广告营销/产品广告/产品功能演示
```

### 6.3 归档状态

当前程序正式使用：

- `已归档`
- `已下载未归档`

判定规则：

- 如果存在唯一有效 `内容题材` 路径且归档成功，写 `已归档`
- 如果下载成功但无法确定唯一归档路径，写 `已下载未归档`

### 6.4 归档文件名

- `归档文件名` 保存最终归档后的文件名
- 在 `.numbers` 中，当前程序会把该单元格写成**纯文本文件名**
- `.numbers` 当前不保证“点击文件名直接打开本地归档文件”
- 如果需要可点击本地文件链接，请使用程序内总表 `xlsx` 或其他 `xlsx` 目标表

示例值：

```text
刷短视频现状_720P_260426_000026.mp4
```

---

## 7. 程序驱动方式

当用户只上传一张 `.numbers` 表格时，当前主流程按下面方式运行：

1. 读取表头
2. 跳过模板说明行
3. 从 `URL` 列提取有效链接
4. 建立任务记录
5. 下载视频
6. 对视频执行**视频级检测打标**
7. 生成多分支正式标签结果
8. 将结构化结果回填到：
   - `归档状态`
   - `一级标签`
   - `二级标签`
   - `三级标签`
   - `四级标签`
   - `归档路径`
   - `归档文件名`
9. 按 `内容题材` 的唯一正式路径把文件复制归档到 `视频数据归档库/内容题材/...`

---

## 8. 显示层限制

当前 `.numbers` 主流程已经支持：

- 可视回填内容
- 保存回填后的文档
- 保留原有列结构
- 自动补写第 `9` 列 `归档文件名`

当前仍然**不支持**：

- 根据 `0级标签` 自动给单元格写背景色
- 按你的模板色卡自动着色
- 把本地归档文件写成 `Numbers` 内可点击的本地文件超链接

这意味着：

- 逻辑分支已经正确回填
- 颜色语义目前仍靠前缀文本表达，而不是靠自动样式表达

---

## 9. 推荐正式模板要求

如果 `.numbers` 要作为正式长期使用模板，建议满足：

- 第 `1` 行固定为正式表头
- 第 `2` 行固定为说明行
- 数据从第 `3` 行开始
- 保持以下列不变：
  - `URL`
  - `采集人`
  - `归档状态`
  - `一级标签`
  - `二级标签`
  - `三级标签`
  - `四级标签`
  - `归档路径`
  - `归档文件名`

不建议：

- 临时改动这些列名
- 在前 9 列中插入新的业务列但不更新程序读取规则
- 把多个标签拆到多套重复层级列中
- 让非 `内容题材` 分支参与目录设计

---

## 10. 当前正式结论

当前 `.numbers` 模板在程序中的正式语义已经固定为：

- **多分支识别**
- **多分支结构化回表**
- **唯一内容题材归档**

也就是说：

- 回表层追求信息完整
- 归档层追求路径唯一

这两层不再混用。
